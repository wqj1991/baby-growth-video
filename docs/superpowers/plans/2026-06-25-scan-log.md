# 扫描实时日志功能实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为扫描照片和视频功能增加实时日志滚动显示，用户可以在扫描过程中看到每个文件的处理状态，并支持展开查看完整日志。

**Architecture:** 采用 Tauri 事件系统，后端扫描过程中通过 `window.emit()` 向前端实时推送日志事件，前端监听事件并实时更新日志列表。使用独立的 ScanLogPanel 组件展示日志，支持收起/展开、自动滚动、级别过滤等功能。

**Tech Stack:** Rust (Tauri) + React + TypeScript + Zustand + Tailwind CSS + Lucide Icons

---

## 文件结构总览

| 文件 | 操作 | 职责 |
|------|------|------|
| `src-tauri/src/media.rs` | 修改 | 添加日志事件结构体、辅助函数，改造扫描函数 emit 事件 |
| `src-tauri/src/main.rs` | 修改 | 更新 scan_media_folder 命令签名，注入 Window |
| `src/types/index.ts` | 修改 | 添加 ScanLog 类型，更新 ScanResult |
| `src/utils/tauriCommands.ts` | 修改 | 添加事件监听工具函数 |
| `src/store/createProjectStore.ts` | 修改 | 添加日志相关状态和 action |
| `src/components/ScanLogPanel.tsx` | 新建 | 日志面板组件 |
| `src/pages/create-project/Step3SelectFolder.tsx` | 修改 | 集成语日面板，移除模拟进度 |

---

## Task 1: 后端 - 添加日志事件结构体和辅助函数

**Files:**
- Modify: `src-tauri/src/media.rs`

### 步骤

- [ ] **Step 1: 在 media.rs 顶部添加 ScanLogEvent 结构体和序列化支持**

在 `const VIDEO_EXTENSIONS` 之前添加：

```rust
use serde::Serialize;

#[derive(Serialize)]
struct ScanLogEvent {
    level: String,
    message: String,
    timestamp: i64,
    file_name: Option<String>,
}

impl ScanLogEvent {
    fn new(level: &str, message: String, file_name: Option<String>) -> Self {
        Self {
            level: level.to_string(),
            message,
            timestamp: chrono::Local::now().timestamp_millis(),
            file_name,
        }
    }
}

fn emit_scan_log(window: &tauri::Window, level: &str, message: String, file_name: Option<String>) {
    let event = ScanLogEvent::new(level, message, file_name);
    if let Err(e) = window.emit("scan://log", &event) {
        eprintln!("Failed to emit scan log event: {}", e);
    }
}
```

注意：需要检查 Cargo.toml 中是否有 serde 和 chrono 依赖，如果没有需要添加。

---

## Task 2: 后端 - 改造 scan_media_folder 函数

**Files:**
- Modify: `src-tauri/src/media.rs`

### 步骤

- [ ] **Step 1: 修改 scan_media_folder 函数签名，增加 window 参数**

将函数签名从：
```rust
pub fn scan_media_folder(db: &Database, project_id: i64, folder: &str) -> Result<ScanResult, Box<dyn std::error::Error>>
```

改为：
```rust
pub fn scan_media_folder(db: &Database, project_id: i64, folder: &str, window: tauri::Window) -> Result<ScanResult, Box<dyn std::error::Error>>
```

- [ ] **Step 2: 在函数开头 emit 扫描开始事件**

在 `let mut total_photos = 0i64;` 之前添加：

```rust
emit_scan_log(&window, "info", format!("开始扫描文件夹: {}", folder), None);
```

- [ ] **Step 3: 在日期识别失败处 emit warn 事件**

在 `skipped_no_date_photos += 1;` 或 `skipped_no_date_videos += 1;` 之后，`continue;` 之前添加：

```rust
emit_scan_log(
    &window,
    "warn",
    format!("⚠ 无法识别日期: {}", file_name),
    Some(file_name.clone()),
);
```

- [ ] **Step 4: 在周期不匹配处 emit warn 事件**

在 `skipped_no_period_photos += 1;` 或 `skipped_no_period_videos += 1;` 之后，`continue;` 之前添加：

```rust
emit_scan_log(
    &window,
    "warn",
    format!("⚠ 日期不在周期内: {}", file_name),
    Some(file_name.clone()),
);
```

- [ ] **Step 5: 在复制失败处 emit error 事件**

在 `skipped_copy_failed_photos += 1;` 或 `skipped_copy_failed_videos += 1;` 之后，`continue;` 之前添加：

```rust
emit_scan_log(
    &window,
    "error",
    format!("✗ 复制失败: {} - {}", file_name, e),
    Some(file_name.clone()),
);
```

- [ ] **Step 6: 在重复文件处 emit warn 事件**

在 `skipped_duplicate_photos += 1;` 或 `skipped_duplicate_videos += 1;` 之后，`continue;` 之前添加：

```rust
emit_scan_log(
    &window,
    "warn",
    format!("⚠ 跳过重复: {}", file_name),
    Some(file_name.clone()),
);
```

- [ ] **Step 7: 在成功添加照片/视频处 emit success 事件**

在 `photos.push(photo);` 之后添加：

```rust
emit_scan_log(
    &window,
    "success",
    format!("✓ 已识别照片: {} ({})", file_name, date_str.clone().unwrap_or_default()),
    Some(file_name.clone()),
);
```

在 `videos.push(video);` 之后添加：

```rust
emit_scan_log(
    &window,
    "success",
    format!("✓ 已识别视频: {} ({})", file_name, date_str.clone().unwrap_or_default()),
    Some(file_name.clone()),
);
```

- [ ] **Step 8: 在函数末尾 emit 扫描完成事件**

在 `Ok(ScanResult { ... })` 之前添加：

```rust
let total_files = total_photos + total_videos;
emit_scan_log(&window, "info", format!("扫描完成，共处理 {} 个文件", total_files), None);
```

---

## Task 3: 后端 - 更新 main.rs 命令注册

**Files:**
- Modify: `src-tauri/src/main.rs`

### 步骤

- [ ] **Step 1: 更新 scan_media_folder 命令签名**

将命令从：
```rust
#[tauri::command]
fn scan_media_folder(
    project_id: i64,
    folder_path: String,
    state: State<AppState>,
) -> Result<media::ScanResult, String> {
    let db = state.db.lock().map_err(|e| e.to_string())?;
    media::scan_media_folder(&db, project_id, &folder_path).map_err(|e| e.to_string())
}
```

改为：
```rust
#[tauri::command]
fn scan_media_folder(
    project_id: i64,
    folder_path: String,
    window: tauri::Window,
    state: State<AppState>,
) -> Result<media::ScanResult, String> {
    let db = state.db.lock().map_err(|e| e.to_string())?;
    media::scan_media_folder(&db, project_id, &folder_path, window).map_err(|e| e.to_string())
}
```

注意：Tauri 的 `generate_handler!` 宏会自动处理 Window 参数的注入，不需要额外配置。

- [ ] **Step 2: 验证编译**

运行 `cargo check` 确保没有编译错误。

---

## Task 4: 前端 - 添加类型定义

**Files:**
- Modify: `src/types/index.ts`

### 步骤

- [ ] **Step 1: 添加 ScanLog 接口**

在 `ScanResult` 接口之前添加：

```typescript
// 扫描日志
export interface ScanLog {
  id: string;
  level: 'success' | 'warn' | 'error' | 'info';
  message: string;
  timestamp: number;
  fileName?: string;
}
```

- [ ] **Step 2: 更新 ScanResult 接口，添加复制失败字段**

在 `skipped_no_period_videos: number;` 之后添加：

```typescript
  skipped_copy_failed_photos: number;
  skipped_copy_failed_videos: number;
```

---

## Task 5: 前端 - 添加事件监听工具函数

**Files:**
- Modify: `src/utils/tauriCommands.ts`

### 步骤

- [ ] **Step 1: 导入事件监听 API**

在文件顶部添加：
```typescript
import { listen } from '@tauri-apps/api/event';
import type { ScanLog } from '../types';
```

- [ ] **Step 2: 添加 onScanLog 工具函数**

在 `// ==================== 扫描文件 ====================` 部分添加：

```typescript
// 监听扫描日志事件
export async function onScanLog(
  callback: (log: Omit<ScanLog, 'id'>) => void
): Promise<() => void> {
  const unlisten = await listen<{
    level: ScanLog['level'];
    message: string;
    timestamp: number;
    file_name?: string;
  }>('scan://log', (event) => {
    callback({
      level: event.payload.level,
      message: event.payload.message,
      timestamp: event.payload.timestamp,
      fileName: event.payload.file_name,
    });
  });

  return unlisten;
}
```

---

## Task 6: 前端 - 在 createProjectStore 中添加日志状态

**Files:**
- Modify: `src/store/createProjectStore.ts`

### 步骤

- [ ] **Step 1: 导入 ScanLog 类型**

修改 import 语句，添加 ScanLog：
```typescript
import type { WizardStep, Baby, Period, ScanResult, ScanLog } from '../types';
```

- [ ] **Step 2: 在 state 接口中添加日志相关状态**

在 `isScanning: boolean;` 之后添加：

```typescript
  // 扫描日志
  scanLogs: ScanLog[];
  isLogExpanded: boolean;
  autoScrollLog: boolean;
```

- [ ] **Step 3: 在 Actions 部分添加日志相关方法**

在 `setIsScanning: (scanning: boolean) => void;` 之后添加：

```typescript
  addScanLog: (log: Omit<ScanLog, 'id'>) => void;
  clearScanLogs: () => void;
  toggleLogExpanded: () => void;
  toggleAutoScrollLog: () => void;
```

- [ ] **Step 4: 初始化状态默认值**

在 `isScanning: false,` 之后添加：

```typescript
  scanLogs: [],
  isLogExpanded: false,
  autoScrollLog: true,
```

- [ ] **Step 5: 实现 action 方法**

在 `setIsScanning: (scanning) => set({ isScanning: scanning }),` 之后添加：

```typescript
  addScanLog: (log) =>
    set((state) => ({
      scanLogs: [
        ...state.scanLogs,
        {
          ...log,
          id: `${log.timestamp}-${state.scanLogs.length}`,
        },
      ],
    })),

  clearScanLogs: () => set({ scanLogs: [] }),

  toggleLogExpanded: () =>
    set((state) => ({ isLogExpanded: !state.isLogExpanded })),

  toggleAutoScrollLog: () =>
    set((state) => ({ autoScrollLog: !state.autoScrollLog })),
```

- [ ] **Step 6: 在 reset 方法中重置日志状态**

在 `reset` 函数的 set 对象中添加：

```typescript
      scanLogs: [],
      isLogExpanded: false,
      autoScrollLog: true,
```

---

## Task 7: 前端 - 创建 ScanLogPanel 组件

**Files:**
- Create: `src/components/ScanLogPanel.tsx`

### 步骤

- [ ] **Step 1: 创建组件文件，导入依赖**

```tsx
import { useEffect, useRef, useState } from 'react';
import {
  CheckCircle,
  AlertTriangle,
  XCircle,
  Info,
  ChevronDown,
  ChevronUp,
  Copy,
  Filter,
} from 'lucide-react';
import type { ScanLog } from '../types';

interface ScanLogPanelProps {
  logs: ScanLog[];
  isExpanded: boolean;
  onToggleExpand: () => void;
  autoScroll: boolean;
  onToggleAutoScroll: () => void;
}

const levelConfig = {
  success: {
    icon: CheckCircle,
    color: 'text-green-500',
    bgColor: 'bg-green-50',
  },
  warn: {
    icon: AlertTriangle,
    color: 'text-yellow-500',
    bgColor: 'bg-yellow-50',
  },
  error: {
    icon: XCircle,
    color: 'text-red-500',
    bgColor: 'bg-red-50',
  },
  info: {
    icon: Info,
    color: 'text-blue-500',
    bgColor: 'bg-blue-50',
  },
};

type LogLevel = keyof typeof levelConfig;
```

- [ ] **Step 2: 实现主组件**

```tsx
export default function ScanLogPanel({
  logs,
  isExpanded,
  onToggleExpand,
  autoScroll,
  onToggleAutoScroll,
}: ScanLogPanelProps) {
  const logContainerRef = useRef<HTMLDivElement>(null);
  const [filter, setFilter] = useState<LogLevel | 'all'>('all');

  // 过滤日志
  const filteredLogs = filter === 'all'
    ? logs
    : logs.filter((log) => log.level === filter);

  // 自动滚动
  useEffect(() => {
    if (autoScroll && logContainerRef.current) {
      logContainerRef.current.scrollTop = logContainerRef.current.scrollHeight;
    }
  }, [filteredLogs, autoScroll]);

  // 复制全部日志
  const handleCopyAll = async () => {
    const text = filteredLogs
      .map((log) => `[${log.level.toUpperCase()}] ${log.message}`)
      .join('\n');
    try {
      await navigator.clipboard.writeText(text);
    } catch (e) {
      console.error('复制失败:', e);
    }
  };

  // 格式化时间
  const formatTime = (timestamp: number) => {
    const date = new Date(timestamp);
    return date.toLocaleTimeString('zh-CN', {
      hour: '2-digit',
      minute: '2-digit',
      second: '2-digit',
    });
  };
```

- [ ] **Step 3: 实现渲染部分**

```tsx
  return (
    <div className="border border-gray-200 rounded-lg overflow-hidden bg-white">
      {/* 工具栏 - 展开时显示 */}
      {isExpanded && (
        <div className="flex items-center justify-between px-4 py-2 bg-gray-50 border-b border-gray-200">
          <div className="flex items-center gap-2">
            <Filter className="w-4 h-4 text-gray-500" />
            <select
              value={filter}
              onChange={(e) => setFilter(e.target.value as LogLevel | 'all')}
              className="text-sm border border-gray-300 rounded px-2 py-1 bg-white"
            >
              <option value="all">全部</option>
              <option value="success">成功</option>
              <option value="warn">警告</option>
              <option value="error">错误</option>
              <option value="info">信息</option>
            </select>
          </div>

          <div className="flex items-center gap-3">
            <label className="flex items-center gap-1 text-sm text-gray-600 cursor-pointer">
              <input
                type="checkbox"
                checked={autoScroll}
                onChange={onToggleAutoScroll}
                className="rounded"
              />
              自动滚动
            </label>
            <button
              onClick={handleCopyAll}
              className="flex items-center gap-1 text-sm text-gray-600 hover:text-gray-900"
              title="复制全部日志"
            >
              <Copy className="w-4 h-4" />
              复制
            </button>
          </div>
        </div>
      )}

      {/* 日志列表 */}
      <div
        ref={logContainerRef}
        className={`overflow-y-auto font-mono text-sm ${
          isExpanded ? 'max-h-[60vh]' : 'max-h-[120px]'
        }`}
      >
        {filteredLogs.length === 0 ? (
          <div className="p-4 text-center text-gray-400">
            暂无日志
          </div>
        ) : (
          filteredLogs.map((log) => {
            const config = levelConfig[log.level];
            const Icon = config.icon;
            return (
              <div
                key={log.id}
                className={`flex items-start gap-2 px-4 py-1.5 hover:bg-gray-50 ${config.bgColor}`}
                title={log.fileName}
              >
                <Icon className={`w-4 h-4 flex-shrink-0 mt-0.5 ${config.color}`} />
                <span className="flex-1 text-gray-700 break-all">
                  {log.message}
                </span>
                <span className="text-gray-400 text-xs flex-shrink-0">
                  {formatTime(log.timestamp)}
                </span>
              </div>
            );
          })
        )}
      </div>

      {/* 底部展开/收起按钮 */}
      <div className="flex items-center justify-between px-4 py-2 bg-gray-50 border-t border-gray-200">
        <span className="text-xs text-gray-500">
          共 {logs.length} 条日志
          {filter !== 'all' && ` (显示 ${filteredLogs.length} 条)`}
        </span>
        <button
          onClick={onToggleExpand}
          className="flex items-center gap-1 text-sm text-primary-600 hover:text-primary-700"
        >
          {isExpanded ? (
            <>
              收起
              <ChevronUp className="w-4 h-4" />
            </>
          ) : (
            <>
              展开查看全部日志
              <ChevronDown className="w-4 h-4" />
            </>
          )}
        </button>
      </div>
    </div>
  );
}
```

---

## Task 8: 前端 - 集成到 Step3SelectFolder

**Files:**
- Modify: `src/pages/create-project/Step3SelectFolder.tsx`

### 步骤

- [ ] **Step 1: 导入新组件和工具**

在文件顶部添加：
```typescript
import { useEffect, useRef } from 'react';
import ScanLogPanel from '../../components/ScanLogPanel';
import { onScanLog } from '../../utils/tauriCommands';
```

- [ ] **Step 2: 从 store 中获取日志相关状态和方法**

在 `useCreateProjectStore()` 的解构中添加：
```typescript
    scanLogs,
    isLogExpanded,
    autoScrollLog,
    addScanLog,
    clearScanLogs,
    toggleLogExpanded,
    toggleAutoScrollLog,
```

- [ ] **Step 3: 添加 unlisten ref**

在 `const [scanProgress, setScanProgress] = useState(0);` 之后添加：
```typescript
  const unlistenScanLogRef = useRef<(() => void) | null>(null);
```

- [ ] **Step 4: 修改 handleScan 函数，添加事件监听**

替换 handleScan 函数：

```typescript
  const handleScan = async () => {
    if (!folderPath) return;

    setIsScanning(true);
    clearScanLogs();

    try {
      // 注册日志事件监听
      const unlisten = await onScanLog((log) => {
        addScanLog(log);
      });
      unlistenScanLogRef.current = unlisten;

      const result: ScanResult = await scanMediaFolder(projectId || 0, folderPath);
      setScanResult(result);
    } catch (error) {
      console.error('扫描失败:', error);
      alert('扫描文件夹失败，请重试');
    } finally {
      setIsScanning(false);
      // 移除事件监听
      if (unlistenScanLogRef.current) {
        unlistenScanLogRef.current();
        unlistenScanLogRef.current = null;
      }
    }
  };
```

- [ ] **Step 5: 添加 useEffect 清理事件监听**

在组件末尾添加：

```typescript
  // 组件卸载时清理事件监听
  useEffect(() => {
    return () => {
      if (unlistenScanLogRef.current) {
        unlistenScanLogRef.current();
      }
    };
  }, []);
```

- [ ] **Step 6: 移除模拟进度条相关代码**

删除：
- `const [scanProgress, setScanProgress] = useState(0);`
- handleScan 中的 `setScanProgress(0);`
- handleScan 中的 `setScanProgress(100);`
- 整个模拟进度的 setInterval 代码块
- handleRescan 中的 `setScanProgress(0);`
- JSX 中的进度条渲染部分（`{isScanning && (...)}` 块）

- [ ] **Step 7: 在扫描按钮下方添加日志面板**

在扫描按钮的 `</div>` 之后，扫描结果之前添加：

```tsx
          {/* 实时日志 */}
          {(isScanning || scanLogs.length > 0) && (
            <div className="mb-6">
              <ScanLogPanel
                logs={scanLogs}
                isExpanded={isLogExpanded}
                onToggleExpand={toggleLogExpanded}
                autoScroll={autoScrollLog}
                onToggleAutoScroll={toggleAutoScrollLog}
              />
            </div>
          )}
```

---

## Task 9: 测试验证

### 步骤

- [ ] **Step 1: 后端编译验证**

运行：
```bash
cd src-tauri
cargo check
```

预期：编译通过，无错误和警告

- [ ] **Step 2: 前端类型检查**

运行：
```bash
npx tsc --noEmit
```

预期：无类型错误

- [ ] **Step 3: 功能测试**

1. 启动应用：`pnpm tauri dev`
2. 进入创建项目向导的步骤3
3. 选择一个包含照片的文件夹
4. 点击"开始扫描"
5. 验证：
   - [ ] 日志面板实时显示每个文件的处理状态
   - [ ] 日志自动滚动到底部
   - [ ] 不同级别的日志有不同颜色和图标
   - [ ] 点击"展开查看全部日志"可以展开
   - [ ] 展开后可以看到过滤选项和自动滚动开关
   - [ ] 关闭自动滚动后日志不会自动滚动
   - [ ] 点击"复制"可以复制日志到剪贴板
   - [ ] 扫描完成后日志保留，可以查看
   - [ ] 点击"重新扫描"会清空旧日志重新开始

---

## 自审检查

### 1. Spec 覆盖检查
- ✅ 实时日志滚动 - Task 2, 5, 6, 7, 8
- ✅ 展开查看完整日志 - Task 7
- ✅ 按级别着色 - Task 7
- ✅ Tauri 事件系统方案 - Task 1, 2, 3, 5
- ✅ 内嵌式展示 - Task 7, 8

### 2. 占位符检查
- ✅ 没有 TBD / TODO
- ✅ 所有代码步骤都有完整代码
- ✅ 所有命令都有具体说明

### 3. 类型一致性检查
- ✅ 后端 ScanLogEvent 字段与前端 ScanLog 字段对应
- ✅ level 枚举值一致（success/warn/error/info）
- ✅ 事件名一致（scan://log）
- ✅ store 方法名一致

### 4. 遗漏任务
- ✅ 后端事件发送
- ✅ 前端事件监听
- ✅ UI 组件
- ✅ 状态管理
- ✅ 集成
- ✅ 测试验证
