name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install pnpm
        run: npm install -g pnpm@10

      - name: Install dependencies
        run: pnpm install

      - name: Build app
        run: pnpm tauri build

      - name: List Windows build artifacts
        run: Get-ChildItem -Path src-tauri\target\release\bundle -Recurse -File | Select-Object -ExpandProperty FullName

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: windows-build
          path: |
            src-tauri/target/release/bundle/**/*.exe
            src-tauri/target/release/bundle/**/*.zip

  build-macos:
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install pnpm
        run: npm install -g pnpm@10

      - name: Install dependencies
        run: pnpm install

      - name: Install ffmpeg for macOS
        run: brew install ffmpeg

      - name: Replace with macOS ffmpeg binaries
        run: |
          rm -f src-tauri/resources/ffmpeg/*.exe
          FFmpeg_PATH=$(brew --prefix ffmpeg)/bin
          cp "$FFmpeg_PATH/ffmpeg" src-tauri/resources/ffmpeg/ffmpeg
          cp "$FFmpeg_PATH/ffprobe" src-tauri/resources/ffmpeg/ffprobe

      - name: Build app
        run: pnpm tauri build

      - name: List macOS build artifacts
        run: find src-tauri/target/release/bundle -type f

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: macos-build
          path: |
            src-tauri/target/release/bundle/**/*.dmg
            src-tauri/target/release/bundle/**/*.tar.gz

  release:
    needs: [build-windows, build-macos]
    runs-on: ubuntu-latest
    steps:
      - name: Download Windows artifacts
        uses: actions/download-artifact@v4
        with:
          name: windows-build
          path: windows

      - name: Download macOS artifacts
        uses: actions/download-artifact@v4
        with:
          name: macos-build
          path: macos

      - name: List downloaded artifacts
        run: |
          ls -la windows/
          ls -la macos/

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            windows/**/*
            macos/**/*